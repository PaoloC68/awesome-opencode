name: opencode-cost-guard
repo: https://github.com/jjmartres/opencode-cost-guard
tagline: Warn or stop your OpenCode session when it hits a spending limit
description: Monitors session cost in real time and fires a warning when you approach your budget, then a hard alert when you cross it. Supports warn and block modes, per-project config overrides, and zero dependencies.
